# 图片替换说明

1. 把证件照放到此文件夹，例如 `profile.jpg`。
2. 把作品图片放到此文件夹，例如 `work-01.jpg`。
3. 打开 `content/site-content.js`，把对应图片链接替换为本地路径：

```js
profilePhoto: 'assets/images/profile.jpg'
```

4. 提交并推送到 GitHub 后，网站会自动更新。

当前页面使用的是网络占位图片，正式投递前请替换为自己的真实作品。
