# lqy’s tiny universe

A small, hand-drawn photography portfolio built with plain HTML, CSS, and JavaScript.

## Local preview

Open `index.html` directly, or run a local static server and visit `http://127.0.0.1:4173`.

## Publishing

GitHub Pages is deployed automatically whenever changes are pushed to `main`.
